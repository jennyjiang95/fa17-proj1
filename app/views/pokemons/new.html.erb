<%= simple_form_for :pokemon, :url => create_path do |f| %>
  <%= f.input :name, label: "Pokemon's name: ", error: 'You must name your Pokemon!!!' %>
  <%= f.button :submit, 'Create New Pokemon' %>
<% end %>
